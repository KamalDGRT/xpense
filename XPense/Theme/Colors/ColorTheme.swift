//
// ColorTheme.swift
// XPense
//

import SwiftUI

protocol ColorTheme {
    var ashGray: Color { get }
    var coalGray: Color { get }
    var ironGray: Color { get }
    var mistGray: Color { get }
    var slateGray: Color { get }

    var amber: Color { get }
    var blue: Color { get }
    var green: Color { get }
    var mutedGray: Color { get }
    var red: Color { get }

    var white: Color { get }
}
